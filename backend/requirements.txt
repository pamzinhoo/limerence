fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
SQLAlchemy[asyncio]>=2.0,<3.0
alembic>=1.13,<2.0
asyncpg>=0.30,<0.32
python-dotenv>=1.0,<2.0
PyJWT>=2.8,<3.0
boto3>=1.34,<2.0
aiohttp>=3.9,<4.0

# Rate limiter / estado compartilhado entre instancias (opcional — sem
# REDIS_URL configurado, core/rate_limiter_factory.py cai pro RateLimiter
# em memoria de sempre, mesmo comportamento de hoje).
redis>=5.0,<6.0
